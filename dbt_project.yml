name: 'enterprise_data_mesh'
version: '1.0.0'
config-version: 2

# Update your flags block to look like this:
flags:
  require_explicit_package_overrides_for_builtin_materializations: false
  warn_error_options:
    silence:
      - PackageMaterializationOverrideDeprecation

profile: 'default'
model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  enterprise_data_mesh:
    
    # 1. STAGING (Shared Compute)
    staging:
      +materialized: view
      +schema: STAGING
      +snowflake_warehouse: ADMIN_WH

    # 2. MARTS (Departmental Data Mesh & FinOps Compute)
    marts:
      +materialized: table
      
      core:
        +schema: CORE_ANALYTICS
        +group: core
        +snowflake_warehouse: ADMIN_WH
        
      finance:
        +schema: FINANCE_ANALYTICS
        +group: finance
        +snowflake_warehouse: FINANCE_WH
        +tags: ["finance_run"]
        
      hr:
        +schema: HR_ANALYTICS
        +group: hr
        +snowflake_warehouse: HR_WH
        +tags: ["hr_run"]

      marketing:
        +schema: MARKETING_ANALYTICS
        +group: marketing
        +snowflake_warehouse: MARKETING_WH
        +tags: ["marketing_run"]

      supply_chain:
        +schema: SUPPLY_CHAIN_ANALYTICS
        +group: supply_chain
        +snowflake_warehouse: SUPPLY_CHAIN_WH
        +tags: ["sc_run"]

  # 3. ELEMENTARY LOGGING 
  elementary:
    +database: ECOMMERCE
    +schema: DBT_INFO
    +snowflake_warehouse: ADMIN_WH